const swiper = new Swiper('#sw-1', {
    // Optional parameters
    slidesPerView:5,
    direction: 'horizontal',
    loop: true,
    autoplay: {
      delay: 2000,
    }
  });
  const swiper2 = new Swiper('#sw-2', {
    // Optional parameters
    slidesPerView:5,
    direction: 'horizontal',
    loop: true,
    autoplay: {
      delay: 2000,
      reverseDirection: true
    }
  });

  